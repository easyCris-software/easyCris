# Darwin runtime pins preserve the validated scientific stack in
# requirements-validated.txt. Do not upgrade this stack without re-running the
# statistical parity validation.
numpy==1.26.4
scipy==1.14.1
pandas==2.3.3
statsmodels==0.14.4
scikit-learn==1.7.2
scikit-posthocs==0.11.4
lmfit==1.3.4
asteval==1.0.7
uncertainties==3.2.3
dill==0.4.0
lifelines==0.30.0
matplotlib==3.10.7
seaborn==0.13.2
plotly==5.24.1
# 0.1.0.post1 publishes Windows-only wheels. 0.2.1 publishes native x86_64
# and arm64 macOS wheels compatible with the retained Plotly 5.24.1 release.
# Do not move to Plotly 6/Kaleido 1 in this port: that introduces a separate
# Chrome runtime that must be distributed, signed, and revalidated.
kaleido==0.2.1
# Darwin uses the system trust store; Windows retains its existing certifi shim.
truststore==0.10.4
# Keep the resolved CA bundle aligned with the generated MPL-2.0 legal record.
certifi==2026.7.22
gseapy==1.1.11
pytest==9.0.1
polars==1.20.0
duckdb==1.1.3
