# Python Dependencies - VALIDATED VERSIONS
# Date: 2026-01-28
# Python: 3.12
# Validation Plan: v3.14 (37/40 tests, 645/651 metrics)
#
# CRITICAL: These are the EXACT versions used for R parity validation.
# Do NOT upgrade without re-running validation tests.
#
# Install: python_embedded/python.exe -m pip install -r requirements-validated.txt --target python_embedded/python_dependencies/
#
# ============================================================================
# MPL-2.0 LICENSE ELIMINATION (2026-01-28)
# ============================================================================
# Two MPL-2.0 packages were replaced with MIT alternatives:
#
# 1. certifi (MPL-2.0) -> MIT stub using Windows certificate store
#    Location: python_embedded/python_dependencies/certifi/
#    Implementation: Extracts CA certs via ssl.enum_certificates()
#    API: certifi.where() and certifi.contents() work as expected
#
# 2. legacy-api-wrap (MPL-2.0) -> REMOVED (anndata has built-in fallback)
#    anndata/compat/__init__.py lines 198-205 checks for legacy_api_wrap
#    If not found, uses no-op decorator: lambda func: func
#    No functional impact - only disables deprecation warnings
#
# Result: ZERO copyleft licenses in Python runtime
# ============================================================================

# TLS/SSL (MIT stub - NOT from pip)
# certifi - custom MIT stub at python_dependencies/certifi/ (Windows cert store)
# legacy-api-wrap - NOT INSTALLED (anndata fallback used)

# Core Scientific Stack (VALIDATED)
numpy==1.26.4
scipy==1.14.1
pandas==2.3.3
statsmodels==0.14.4
scikit-learn==1.7.2
scikit-posthocs==0.11.4

# Dose-Response & Curve Fitting
lmfit==1.3.4
asteval==1.0.7
uncertainties==3.2.3
dill==0.4.0

# Survival Analysis
lifelines==0.30.0

# Visualization
matplotlib==3.10.7
seaborn==0.13.2
kaleido==0.1.0.post1
# truststore - REMOVED (replaced by certifi MIT stub using Windows cert store)
#RNAseq

Pydeseq2 and its deps
# Gene Set Enrichment
gseapy==1.1.11

# Testing
pytest==9.0.1

# Phase 5: Large Dataset Support (Optional - for DuckDB hybrid cache)
# These do NOT affect validated statistical tests.
# Only used when dataset >= 1M rows or file >= 500MB.
polars==1.20.0
duckdb==1.1.3

# OLE Clipboard Support (for PowerPoint/Excel linking)
# IMPORTANT: After installation, copy DLLs to embedded Python directory:
#   cp python_dependencies/pywin32_system32/pythoncom312.dll python_embedded/
#   cp python_dependencies/pywin32_system32/pywintypes312.dll python_embedded/
pywin32==311
